let catStr = "catStr";
let batStr = "batStr";
let matStr = "matStr";
let atRegex = /[a-z]at/;
console.log(catStr.match(atRegex));
console.log(batStr.match(atRegex));
console.log(matStr.match(atRegex));
